import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,it}from'node:test';const src=(p:string)=>readFileSync(join(process.cwd(),'src',p),'utf8');
describe('core record compose',()=>{it('requires viewing method independently from media type and forwards movie tv multi',()=>{const composer=src('components/core/RecordComposer.tsx');const hook=src('hooks/useMediaSearch.ts');assert.match(composer,/ViewingMethodControl/);assert.match(composer,/MediaTypeControl/);assert.match(hook,/searchMedia\(\{ query: trimmedQuery, type/)});it('keeps a per-user session draft and idempotent request id through retry',()=>{const code=src('components/core/RecordComposer.tsx');assert.match(code,/davas:draft:\$\{id\}/);assert.match(code,/sessionStorage/);assert.match(code,/crypto\.randomUUID/);assert.match(code,/allowDuplicate/);assert.match(code,/POSSIBLE_REWATCH/)});it('supports nullable radio rating and omits removed compose fields',()=>{const code=src('components/core/RecordComposer.tsx');assert.match(code,/type="radio"/);assert.match(code,/\[null,\s*1,\s*2,\s*3,\s*4,\s*5\]/);assert.doesNotMatch(code,/mood|memoryNote|companions|watchedPlace|tags/)})});
