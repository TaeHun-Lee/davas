import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,it}from'node:test';const src=(p:string)=>readFileSync(join(process.cwd(),'src',p),'utf8');
describe('four-tab core shell',()=>{it('renders exactly the four navigation labels with home first and no drawer',()=>{const code=src('components/core/CoreUi.tsx');const tabContract=code.slice(code.indexOf('const tabs'),code.indexOf('export function CoreHeader'));for(const label of['홈','기록하기','내 기록','친구'])assert.match(tabContract,new RegExp(label));assert.equal((tabContract.match(/label:/g)??[]).length,4);assert.doesNotMatch(code,/hamburger|drawer|추천|찜|알림/)});it('uses accessible vector icons for home navigation and settings',()=>{const code=src('components/core/CoreUi.tsx');assert.match(code,/CoreNavIcon/);assert.match(code,/data-icon="settings"/);assert.match(code,/aria-label="설정 열기"/);assert.doesNotMatch(code,/>\s*설정\s*</)});it('keeps home task-first without a redundant introduction panel',()=>{const feed=src('components/core/RecordScreens.tsx');assert.match(feed,/<h1 className="sr-only">홈<\/h1>/);assert.match(feed,/home-search-link/);assert.doesNotMatch(feed,/home-intro|친구의 감상과 오늘의 추천을 한곳에서/)});it('provides padded movie and drama carousel controls',()=>{const recommendations=src('components/core/HomeRecommendations.tsx');const css=src('app/globals.css');assert.match(recommendations,/carouselRef/);assert.match(recommendations,/scrollBy/);assert.match(recommendations,/이전.*추천/);assert.match(recommendations,/다음.*추천/);assert.match(css,/scroll-snap-type: x mandatory/);assert.doesNotMatch(css,/home-recommendation-row \{[^}]*margin-(right|left):-/)});it('starts a record with the selected recommendation even when a draft exists',()=>{const recommendations=src('components/core/HomeRecommendations.tsx');const composer=src('components/core/RecordComposer.tsx');assert.match(recommendations,/\/records\/new\?mediaId=/);assert.doesNotMatch(recommendations,/\/explore\?detail=/);assert.ok(composer.indexOf("const mediaId = params.get('mediaId')")<composer.indexOf('const saved = sessionStorage.getItem'));assert.match(composer,/resumedDraft\.selected\s*=/)});it('keeps independent compact recommendation and friend-feed errors',()=>{const feed=src('components/core/RecordScreens.tsx');assert.match(feed,/home-feed-message/);assert.match(feed,/<RecordList scope="friends" compact \/>/);assert.match(feed,/<HomeRecommendations \/>/)});it('keeps the 430px shell, safe area and focus treatment in shared styles',()=>{const css=src('app/globals.css');assert.match(css,/max-width: 430px/);assert.match(css,/safe-area-inset-bottom/);assert.match(css,/:focus-visible/);assert.match(css,/--commit: #d83b35/)})});
