import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,it}from'node:test';const src=(path:string)=>readFileSync(join(process.cwd(),'src',path),'utf8');
describe('Davas private friend records IA',()=>{it('uses root for friend records and redirects legacy public community routes',()=>{assert.match(src('app/page.tsx'),/FeedScreen/);assert.match(src('app/feed/page.tsx'),/redirect\('\/'\)/);assert.match(src('app/community/page.tsx'),/redirect\('\/'\)/)});it('keeps public-SNS reactions, comments, popularity and author profiles out of the active record screen',()=>{const core=src('components/core/RecordScreens.tsx');assert.doesNotMatch(core,/CommunityComments|Reaction|Popular|followCommunity|likeCommunity/);assert.match(core,/RECORD_NOT_FOUND/)})});
