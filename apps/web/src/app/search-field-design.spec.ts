import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,it}from'node:test';const src=(p:string)=>readFileSync(join(process.cwd(),'src',p),'utf8');
describe('record search accessibility',()=>{it('keeps both filter rows expanded with pressed semantics',()=>{const ui=src('components/core/CoreUi.tsx');const screens=src('components/core/RecordScreens.tsx');assert.match(ui,/aria-pressed/);assert.match(screens,/작품 종류/);assert.match(screens,/본 곳/);assert.match(screens,/mediaType/);assert.match(screens,/viewingMethod/)});it('uses different friend and mine placeholders and URL query state',()=>{const code=src('components/core/RecordScreens.tsx');assert.match(code,/작품 제목 또는 친구 이름/);assert.match(code,/작품 제목으로 내 기록 찾기/);assert.match(code,/URLSearchParams/)})});
