'use client';
import Link from 'next/link';import { useRouter } from 'next/navigation';import { useEffect,useState } from 'react';import { getMe,logout,normalizeProfileImageUrl } from '../../lib/api/auth';import { deleteMe,deleteProfileImage,updateMe,uploadProfileImage } from '../../lib/api/users';import { purgeSessionDrafts } from '../../lib/api/core';import { AsyncState,TaskShell } from '../core/CoreUi';
export function SettingsScreen(){const router=useRouter();const[user,setUser]=useState<Awaited<ReturnType<typeof getMe>>|null>(null);const[nickname,setNickname]=useState('');const[busy,setBusy]=useState('');const[error,setError]=useState('');const[deleteOpen,setDeleteOpen]=useState(false);const[password,setPassword]=useState('');useEffect(()=>{getMe().then((value)=>{setUser(value);setNickname(value.nickname)}).catch(()=>setError('프로필을 불러오지 못했어요.'))},[]);if(!user)return <TaskShell title="설정" fallback="/">{error?<p className="form-error">{error}</p>:<AsyncState kind="loading"/>}</TaskShell>;const save=async()=>{setBusy('save');setError('');try{const next=await updateMe({nickname});setUser(next)}catch{setError('변경 내용을 저장하지 못했어요.')}finally{setBusy('')}};const signout=async()=>{setBusy('logout');await logout().catch(()=>undefined);purgeSessionDrafts();router.replace('/login')};const remove=async()=>{setBusy('delete');setError('');try{await deleteMe(password);purgeSessionDrafts();router.replace('/login')}catch(cause){setError(cause instanceof Error?cause.message:'계정을 삭제하지 못했어요.')}finally{setBusy('')}};return <TaskShell title="설정" fallback="/"><section className="core-card p-5"><h1 className="section-title">프로필</h1><div className="mt-4 flex items-center gap-4">{user.profileImageUrl?<img src={normalizeProfileImageUrl(user.profileImageUrl)??''} alt="내 프로필" className="h-16 w-16 rounded-full object-cover"/>:<span className="flex h-16 w-16 items-center justify-center rounded-full bg-[var(--blue-soft)] text-2xl font-black text-[var(--blue)]">{user.nickname.slice(0,1)}</span>}<div className="flex flex-wrap gap-2"><label className="secondary-button cursor-pointer">사진 선택<input className="sr-only" type="file" accept="image/jpeg,image/png,image/webp" onChange={async(event)=>{const file=event.target.files?.[0];if(!file)return;setBusy('image');try{setUser(await uploadProfileImage(file))}catch{setError('사진을 저장하지 못했어요.')}finally{setBusy('')}}}/></label>{user.profileImageUrl?<button className="danger-button" onClick={async()=>{setUser(await deleteProfileImage())}}>사진 삭제</button>:null}</div></div><label className="mt-5 block"><span className="field-label">닉네임</span><input className="text-input" minLength={2} maxLength={20} value={nickname} onChange={(event)=>setNickname(event.target.value)}/></label><button className="commit-button mt-4" disabled={busy==='save'||nickname.trim()===user.nickname||nickname.trim().length<2} onClick={save}>{busy==='save'?'저장 중…':'변경 내용 저장'}</button>{error?<p className="form-error mt-3">{error}</p>:null}</section><section className="core-card mt-5 divide-y divide-[var(--border)] px-4"><Link className="flex min-h-14 items-center justify-between text-sm font-bold text-[var(--heading)]" href="/terms">이용약관 <span>›</span></Link><Link className="flex min-h-14 items-center justify-between text-sm font-bold text-[var(--heading)]" href="/privacy">개인정보처리방침 <span>›</span></Link><button className="flex min-h-14 w-full items-center text-sm font-bold text-[var(--blue)]" disabled={busy==='logout'} onClick={signout}>로그아웃</button></section><section className="core-card mt-5 p-5"><h2 className="section-title text-[var(--danger)]">위험 영역</h2><p className="page-description">계정을 삭제하면 기록과 친구 연결을 되돌릴 수 없어요.</p><button className="danger-button mt-4 w-full" onClick={()=>setDeleteOpen(true)}>계정 삭제</button></section>{deleteOpen?<section role="dialog" aria-modal="true" className="core-card mt-4 p-5"><h2 className="section-title">계정을 삭제할까요?</h2><p className="page-description">확인을 위해 현재 비밀번호를 입력해 주세요.</p><label className="mt-4 block"><span className="field-label">비밀번호</span><input autoFocus className="text-input" type="password" value={password} onChange={(event)=>setPassword(event.target.value)}/></label><div className="mt-4 grid grid-cols-2 gap-2"><button className="secondary-button" onClick={()=>setDeleteOpen(false)}>취소</button><button className="danger-button" disabled={busy==='delete'||password.length<8} onClick={remove}>계정 삭제</button></div></section>:null}</TaskShell>}
