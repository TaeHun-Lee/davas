import { legalDocuments } from '../../content/legal';import { TaskShell } from '../core/CoreUi';
export function LegalScreen({kind}:{kind:'terms'|'privacy'}){const document=legalDocuments[kind];return <TaskShell title={document.title} fallback="/"><article className="core-card p-5"><div className="rounded-2xl bg-[var(--danger-bg)] p-4 text-sm font-bold leading-6 text-[var(--danger)]">개발용 fixture — 운영 승인 원문으로 교체하기 전에는 배포할 수 없습니다.</div><p className="page-description mt-4">버전 {document.version}</p>{document.sections.map((section)=><section className="mt-6" key={section.heading}><h1 className="section-title">{section.heading}</h1><p className="mt-2 text-sm font-semibold leading-6 text-[var(--text)]">{section.body}</p></section>)}</article></TaskShell>}
