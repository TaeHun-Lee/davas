import assert from'node:assert/strict';import{describe,it}from'node:test';import type{QueryRunner}from'typeorm';import{CoreRecordContract1720670500000}from'./1720670500000-CoreRecordContract';import{FriendInvitesAndConsents1720670600000}from'./1720670600000-FriendInvitesAndConsents';
async function statements(migration:{up(runner:QueryRunner):Promise<void>;down(runner:QueryRunner):Promise<void>}){const up:string[]=[];await migration.up({query:async(sql:string)=>{up.push(sql)}}as never);const down:string[]=[];await migration.down({query:async(sql:string)=>{down.push(sql)}}as never);return{up:up.join('\n'),down:down.join('\n')}}
describe('core additive migrations',()=>{it('adds two-axis record fields nullable legacy support idempotency and deterministic cursors',async()=>{const sql=await statements(new CoreRecordContract1720670500000());for(const field of['viewing_method','shared_at','client_request_id','client_request_fingerprint'])assert.match(sql.up,new RegExp(field));assert.match(sql.up,/ALTER COLUMN "rating" DROP NOT NULL/);assert.match(sql.up,/IDX_diary_user_client_request/);assert.match(sql.up,/영화관|극장/);assert.match(sql.up,/netflix|넷플릭스/);assert.match(sql.up,/visibility" IN \('FRIENDS', 'SELECTED'\)/);assert.match(sql.down,/DROP COLUMN IF EXISTS "viewing_method"/)});it('creates separate hashed friend invites and versioned consent storage',async()=>{const sql=await statements(new FriendInvitesAndConsents1720670600000());assert.match(sql.up,/friend_invites/);assert.match(sql.up,/token_hash/);assert.doesNotMatch(sql.up,/"token" varchar/);assert.match(sql.up,/used_by_user_id/);assert.match(sql.up,/user_consents/);assert.match(sql.up,/terms_version/);assert.match(sql.down,/DROP TABLE IF EXISTS "friend_invites"/)})});
