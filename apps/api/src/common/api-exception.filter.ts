import { ArgumentsHost,Catch,ExceptionFilter,HttpException,HttpStatus } from '@nestjs/common';import type{Response}from'express';
@Catch()
export class ApiExceptionFilter implements ExceptionFilter{catch(exception:unknown,host:ArgumentsHost){const response=host.switchToHttp().getResponse<Response>();const status=exception instanceof HttpException?exception.getStatus():HttpStatus.INTERNAL_SERVER_ERROR;const raw=exception instanceof HttpException?exception.getResponse():null;if(raw&&typeof raw==='object'&&'code'in raw){response.status(status).json(raw);return}const source=typeof raw==='string'?raw:raw&&typeof raw==='object'&&'message'in raw?(raw as{message:string|string[]}).message:status>=500?'서버에서 요청을 처리하지 못했어요.':'요청을 확인해 주세요.';const message=Array.isArray(source)?source.join(' '):source;response.status(status).json({statusCode:status,code:status===400?'VALIDATION_ERROR':status===401?'UNAUTHORIZED':status===404?'NOT_FOUND':status>=500?'INTERNAL_ERROR':'REQUEST_FAILED',message})}}
