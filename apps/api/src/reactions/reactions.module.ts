import { Module } from '@nestjs/common'; import { TypeOrmModule } from '@nestjs/typeorm'; import { AuthModule } from '../auth/auth.module'; import { DiaryEntity, DiaryReactionEntity } from '../database/entities'; import { DiariesModule } from '../diaries/diaries.module'; import { NotificationsModule } from '../notifications/notifications.module'; import { ReactionsController } from './reactions.controller'; import { ReactionsService } from './reactions.service'; @Module({imports:[AuthModule,DiariesModule,NotificationsModule,TypeOrmModule.forFeature([DiaryEntity,DiaryReactionEntity])],controllers:[ReactionsController],providers:[ReactionsService]}) export class ReactionsModule{}
